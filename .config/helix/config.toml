theme = "onelight"

[editor]
line-number = "relative"
auto-pairs = false

[editor.cursor-shape]
insert = "bar"
normal = "block"
select = "underline"

[keys.insert]
j = { k = "normal_mode" } # Maps `jk` to exit insert mode
